import 'package:flutter/material.dart';

class MyappDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset("assets/photos/allpage.png"),
      ],
    );
  }
}